#ifndef ENUMS_UTILS_H
#define ENUMS_UTILS_H

// Enum for representing implicant bits
enum class ImplicantBit {
    $zero,   // Represents '0'
    $one,    // Represents '1'
    $dash    // Represents '-' (don't care)
};

#endif // ENUMS_UTILS_H